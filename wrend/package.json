{
    "scripts": {
        "test": "wasm-pack test --chrome --headless",
        "build": "RUST_BACKTRACE=full wasm-pack build --out-dir pkg --dev",
        "pack": "wasm-pack pack",
        "publish": "wasm-pack publish"
    }
}